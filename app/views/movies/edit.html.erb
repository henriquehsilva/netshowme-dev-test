<div class="container">
  <div class="row align-items-center" style="margin-top: 100px">
    <div class="col">
      <%= form_with scope: :movie, method: :put, url: user_movies_path(current_user.id, id: @movie.id),local: true, class: 'form-signin' do |f| %>
        <%= render 'form_fields', f: f %>
        <div class="actions">
          <%= f.submit "Editar Vídeo", class: "btn btn-lg btn-dark bg-dark btn-block", type: "submit" %>
        </div>
      <% end %>
    </div>
    <div class="col">
    </div>
  </div>
</div>
